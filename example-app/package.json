{
  "name": "coon-meeting-example-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The minimal standalone product a real integrator would build on coon-meeting-sdk - resolve a join link, prompt for a name, join the call. Nothing else.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "coon-meeting-sdk": "github:Ahmad-S-Nasser/Meeting-frontend",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
